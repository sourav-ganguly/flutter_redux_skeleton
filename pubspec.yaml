name: clean_architecture_demo
description: Clean Architecture for Flutter

version: 1.0.0+1

environment:
  sdk: '>=2.6.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  # Service locator
#  get_it: ^3.1.0
  # Bloc for state management
#  flutter_bloc: ^3.0.0
  meta: '>=1.1.0 <2.0.0'
  redux: ^5.0.0
  flutter_redux: ^0.8.2
  # Value equality
  equatable: ^1.0.1
  # Functional programming thingies
  dartz: ^0.8.9
  # Remote API
  data_connection_checker: ^0.3.4
  http: ^0.11.3+16
  http_middleware: ^1.0.0
  http_logger: ^1.0.0
  # Local cache
  shared_preferences: ^0.5.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^4.1.1

flutter:
  uses-material-design: true
